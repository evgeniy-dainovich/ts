function sin(value) {
  return Math.sin(value)
}

function cos(value) {
  return Math.cos(value)
}

const pi = Math.pi

function sum(a, b, c) {
  return a + b + c
}

module.exports = {
  sin,
  cos,
  pi,
  sum,
}
